Сборка:

```
mkdir -p build && cd build
cmake .. && make -j
```

Запуск (200 заданий на клиент):

```
./task2_server 200
```

Создаёт `client_1.txt`, `client_2.txt`, `client_3.txt` с результатами.

Файл с тестами:

```
./task2_check
```
